# jsa skill runtime dependencies
#
# These are the Python packages required to run the jsa skill orchestrator
# and its supporting scripts. External CLI tools (semgrep, jsluice, playwright,
# tree-sitter-cli) are not listed here — they must be installed separately.

# FSM state machine
python-statemachine>=0.9.0

# AST-based JS chunking (preferred over token-based fallback).
# tree-sitter-javascript must match the tree-sitter major version (0.21.x).
# The splitter lazy-imports these and falls back to token-based chunking
# if they are missing, so installation is recommended but not strictly
# required for the orchestrator to function.
tree-sitter>=0.21.0
tree-sitter-javascript>=0.21.0
