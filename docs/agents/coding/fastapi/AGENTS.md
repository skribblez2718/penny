# FastAPI Feature Index

- [FastAPI Documentation Lookup](fastapi.md): Live-lookup entry point — canonical base (https://fastapi.tiangolo.com/), section→path map, and version check. Fetch current pages at runtime rather than trusting a static list.

> Follows the `docs/agents/coding/library-docs.md` convention: look external docs up live; the local file is an entry point, not a data source.
